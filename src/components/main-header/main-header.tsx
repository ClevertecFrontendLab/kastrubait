import { Layout } from 'antd';

// type IMainHeader = {
//     mobileView: boolean;
// };

// export const SideBar = ({ mobileVew, collapsed, setCollapsed }: IMainHeader) => {
export const MainHeader = () => {
    return (
        <Layout>

        </Layout>
    );
};
